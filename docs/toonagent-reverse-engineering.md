# Toon Agent 기능 역설계와 TO-BE 기준

최종 갱신: 2026-07-17 KST

레퍼런스: `https://app.toonagent.co.kr`

목표: WONY AutoCartoon이 레퍼런스의 개인 창작 워크플로를 기능적으로 동등 이상으로 제공한다.

> 이 문서는 화면과 공개 클라이언트 동작을 기준으로 기능·요청 흐름을 분석한 자료다. 레퍼런스의 소스 코드, 문구, 이미지, 브랜드 자산은 복제하지 않는다. 접속 비밀은 Git에서 제외한 `docs/access-credentials.private.md`에만 둔다.

## 1. 조사 방법과 신뢰도

- 인증 계정으로 홈, 생성기, 자산, 프로젝트, 캔버스, 영상, 커뮤니티, 설정을 순회했다.
- 화면의 입력·상태·오류·폴링 동작과 브라우저에 공개된 요청 계약을 함께 확인했다.
- 결제, 유료 생성, 게시, 삭제, API 키 변경처럼 비용이나 데이터 변경이 생기는 최종 제출은 실행하지 않았다.
- 2026-07-17 재접속 시 레퍼런스 계정의 기기 슬롯 두 개가 모두 사용 중이었다. 어느 세션이 사용자의 현재 세션인지 확정할 수 없어 강제 철회하지 않았고, 같은 날 앞선 인증 조사 결과를 기준선으로 사용했다.
- AutoCartoon은 UI 존재 여부만 보지 않고 Route Handler, Prisma 소유권, 실패 처리, 크레딧 원장, 프로덕션 빌드까지 확인했다.

상태 정의:

- **우위**: 핵심 흐름을 포함하면서 범위·보안·복구 중 하나 이상이 더 낫다.
- **동등**: 사용자가 같은 목적을 끝까지 달성할 수 있다.
- **부분**: 핵심 기반은 있으나 세부 도구나 운영 완성도가 부족하다.
- **미구현**: 대응 흐름이 없다.
- **외부 결정**: 계약, 사업자 정보, 공급자 계정 또는 정책 확정 없이는 완료하면 안 된다.
- **방향 다름**: 의도적으로 다른 사업·기술 모델을 선택했다.

## 2. 레퍼런스 기능 역설계

### 2.1 진입, 계정, 보안

- 이메일/비밀번호, 카카오 로그인
- 회원가입 전 휴대폰 본인확인
- 비밀번호 찾기 시 휴대폰 본인확인 후 새 비밀번호 설정
- 한국어, 영어, 일본어
- 이용약관, 개인정보처리방침, 환불정책
- 기기 최대 2대, 세션 목록과 개별/일괄 철회
- 일반 로그아웃은 브라우저 JWT만 제거하고 서버 기기 세션은 남기는 결함이 관찰됨
- JWT를 `localStorage`에 보관하므로 XSS 토큰 탈취 면에서 HttpOnly 쿠키보다 불리함

### 2.2 홈과 온보딩

- 보유 캐릭터·웹툰 수, 공지, 최근 상태
- `캐릭터 생성 -> 제스처 생성 -> 툰 생성` 3단계 온보딩
- 각 제작 도구로 이동하는 빠른 실행

### 2.3 캐릭터 생성기

- 성별, 연령대, 분위기, 헤어, 의상, 그림 스타일, 배경 여부의 구조화 입력
- 자유 프롬프트 최대 2,000자
- Gemini, Gemini Pro 이미지, OpenAI 이미지와 품질 선택
- 모델·품질별 예상 비용 표시
- 1:1 이미지 생성, 진행 상태, 히스토리, 다운로드, 재생성, 내 캐릭터 저장
- 생성 결과 한 장을 네 방향 슬롯에 복제하는 저장 흐름이 있어 실제 4면도 자동 생성은 아님

### 2.4 내 캐릭터와 보이스

- 이름, 설명, 정면·좌·우·후면 이미지, 장당 최대 10MB
- 기본 캐릭터 지정, 추가·수정·삭제
- 캐릭터당 보이스 최대 3개, 첫 보이스를 영상 기본값으로 사용
- 의미 기반 보이스 검색, 테스트 문장, 미리듣기, 순서 변경, 제거

### 2.5 제스처 생성

- 단일 캐릭터: 캐릭터·스타일·다중 참조·포즈·배경·앵글·모델·품질
- 2인 장면: 캐릭터 A/B, 캐릭터별 행동, 장면 지시, 참조 최대 3장
- 정면, 3/4, 측면, 로우, 하이, 버드아이, 오버숄더, 더치, 클로즈업 등 앵글
- 이전 결과를 편집 소스, 캐릭터, 스타일, 참조로 재사용
- 작업 생성 후 약 3.5초 폴링, queued/processing 복구, 단계 문구, 15분 제한, 재시도 쿨다운
- 커서 기반 히스토리와 무한 스크롤

### 2.6 배경 생성

- 원본 사진, 스타일 이미지, 텍스트 추가 요청, 카메라 앵글, 모델·품질
- 원본이 있으면 스타일 이미지가 필요하고, 원본이 없으면 텍스트가 필요함
- 결과 다운로드와 전용 히스토리

### 2.7 내 생성물

- 전체, 제스처, 캐릭터, 배경, 누끼 필터
- 페이지당 24개, 원본과 별도 썸네일
- 프롬프트, 생성일, 원본 열기, 다운로드, 삭제, 저장 용량 표시

### 2.8 프로젝트와 기획서

- 빈 프로젝트 또는 Markdown 기획서로 시작
- 프로젝트 이름, 대표 썸네일, 컷 수, 수정일, 삭제
- Hook, Relate, Twist, Punchline, CTA 및 컷별 대사·텍스트·장면 지시
- 기획서 분석 후 이미지 일괄 생성
- 고급 플랫폼 화면에는 PDF, 이미지, DOCX, ZIP, TXT, 블로그 자료 가져오기, 캐릭터 별칭·역할, 스타일 참조, 의료광고 준수 옵션이 노출됨
- 목록의 컷 수, 편집 문서 페이지 수, 영상용 사용 가능 컷 수가 서로 다르게 보이는 데이터 정합성 문제가 관찰됨

### 2.9 캔버스 편집기

- 프로젝트 아래 다중 컷/페이지, 추가·이름 변경·순서·복제·삭제·표지 지정
- 자동 저장, 저장 상태, 현재 PNG, 전체 ZIP
- 1080x1080, 1080x1350, 960x1280, 800x1100 규격
- 이동, 크기, 회전, 텍스트, 말풍선, 도형, 펜, 지우개, 크롭
- 레이어 순서·잠금·그룹, 정렬·분배, 가이드
- 폰트·색상·외곽선·정렬·굵기·기울임·밑줄·행간·자간
- 뒤집기, 불투명도, 단색·그라데이션·텍스처 배경
- 워터마크·캡션 스타일 일괄 적용
- 영역 재생성, AI 영역 감지, 인페인트, 캐릭터 교체, OCR/텍스트 제거, 배경 제거
- 전후 비교, 재시도, 적용·취소와 다른 프로젝트 컷 참조
- 기존 프로젝트를 열기만 해도 마이그레이션성 저장으로 수정 시간이 바뀌는 부작용이 관찰됨

### 2.10 영상 만들기

- 직접 이미지 또는 프로젝트 컷 최대 30장
- 컷 순서, 대사, 화자, 캐릭터 기본 보이스
- 세로 숏폼 MP4 생성, 1크레딧=1컷 안내
- 완료 시 카카오 알림톡

### 2.11 툰 광장

- 실명·이메일과 분리한 공개 닉네임
- 작품 공개, 최신/인기 정렬, 게시물, 댓글, 신고

### 2.12 설정과 과금

- Gemini, OpenAI, Anthropic, Remove.bg 키를 서버 저장·마스킹하는 BYOK
- 플랫폼 구독료와 사용자의 외부 AI 비용을 분리
- Personal/Pro 계열 구독, 프로젝트·캔버스·제스처·저장량 제한
- Paddle/NicePay, 프로모션, 주문, 영수증, 저장공간 상품
- 비밀번호 변경, 계정 삭제, 보안 기기 관리

### 2.13 동일 번들의 확장 플랫폼

개인 창작 계정에서 모두 사용할 수 있다고 확정한 범위는 아니지만 공개 라우트와 역할 분기에서 다음 모듈이 확인됐다.

- 관리자·고객·에이전시·워크스페이스·프로모션·결제 관리
- 브랜드/마케팅 분석, 캠페인, 광고, 보고서, GEO, 크롤러
- 업무·스탠드업·TIL·커리어·CRM
- CX 상담원, 지식베이스, 위젯, 운영시간, 예약, 모니터링
- Instagram, Threads, Reddit, Quora, 블로그 예약 게시

이 모듈들은 Toon 제작 기능의 동등성 기준과 분리한다. 사용자의 현재 목표는 개인 창작 서비스이며 Plurank 전체 SaaS 복제는 제품 범위 결정이 먼저다.

## 3. AutoCartoon AS-IS

### 3.1 운영 구조

```text
Browser
  -> Cloud Run / Next.js 15 Route Handlers
  -> Cloud SQL PostgreSQL / Prisma
  -> GenerationJob + GenerationArtifact + CreditLedger
  -> Cloud Tasks
  -> Vertex AI Gemini / Veo + Google Cloud TTS
  -> private GCS + owner-aware media gateway
```

- 운영: `https://wonybananabot-272254743773.asia-northeast3.run.app`
- 로그인: 이메일, 이메일 인증, 12자 임시 비밀번호, 강제 변경, 카카오 OAuth
- 세션: iron-session HttpOnly 쿠키와 DB 기기 세션 최대 2대
- 데이터: 30개 Prisma 모델, 사용자 소유권 기반 개인화
- 생성: Cloud Tasks 비동기 작업, 단계·진행률, 재접속 복구, 재시도, 알림, 자동 환불
- 저장: GCS 원본과 WebP 썸네일, 미디어 게이트웨이 소유권 검사

### 3.2 실제 사용자 기능

- 제작 대시보드, 온보딩, 통계, 최근 작업·프로젝트, 빠른 실행
- 구조화 캐릭터 생성과 1K/2K 품질, 결과 재시도·저장
- 캐릭터 방향별 이미지, 대표/기본 캐릭터, 그룹·마켓, 최대 3개 Chirp 3 HD 보이스
- 1인·2인 제스처 전용 모드와 최대 4명 일반 장면, 방향·앵글·다중 참조
- 저밀도 배경 빠른 생성과 3단계 정리·스타일·앵글 워크플로
- 배경 영속 히스토리에서 과거 결과를 새 워크플로 입력으로 재사용
- 통합 보관함의 타입 필터, 페이지 이동, 썸네일, 저장 용량, 삭제
- 프로젝트·컷·표지·순서·복제·자산·대사·영상 플랜
- Markdown, TXT, CSV, HTML, PDF, DOCX, ZIP, 이미지 OCR, 공개 URL 기획 자료 추출·저장·AI 프로젝트 변환
- 6개 비율 캔버스, 레이어·잠금·그룹·가이드·정렬·분배·회전·뒤집기·불투명도
- 텍스트·말풍선·도형·브러시·지우개·크롭·배경 제거·OCR·영역 AI 다시 그리기
- undo/redo, 자동 저장, 프로젝트 자산 드래그, 현재 PNG와 전체 ZIP
- Veo 3.1 Fast 영상 작업과 브라우저 ffmpeg.wasm 기반 30컷 세로 MP4·캐릭터별 TTS
- 툰 광장 닉네임, 최신/인기, 이미지·링크 게시, 댓글, 글/댓글 좋아요, 신고, 관리자 고정
- 관리자 사용자·크레딧·지식·신고 관리와 독자 기능인 캐릭터 디렉터

## 4. 기능 동등성 매트릭스

| 영역 | 레퍼런스 | AutoCartoon | 상태 | TO-BE/판정 |
| --- | --- | --- | --- | --- |
| 홈·온보딩 | 자산 수, 공지, 3단계 | 통계, 3단계, 최근 작업·프로젝트, 빠른 실행 | 동등 | 공지는 운영 데이터가 생기면 추가 |
| 이메일 인증·복구 | 휴대폰 본인확인 중심 | 이메일 인증, 임시 비밀번호, 강제 변경 | 방향 다름 | 현재 복구 흐름 정상 |
| 카카오 로그인 | 지원 | OAuth, CSRF state, 기존 계정 명시 연결 | 우위 | 이메일 미제공 계정도 안전 연결 가능 |
| 기기 세션 | 2대, 수동 철회, 로그아웃 결함 | 2대, 자동 정리, 목록·개별·일괄 철회, 로그아웃 철회 | 우위 | 유지 |
| 언어 | ko/en/ja | ko | 미구현 | 번역 범위·운영 언어 결정 필요 |
| 법적 문서 | 약관·개인정보·환불 | 없음 | 외부 결정 | 사업자 정보와 법률 검토 필요 |
| 캐릭터 생성 | 구조화 입력, 공급자/품질 | 구조화 입력, 플랫폼 Vertex, 1K/2K, 영속 작업 | 동등 | BYOK는 도입하지 않음 |
| 캐릭터 관리 | 4면, 기본, 보이스 3개 | 4면, 대표·기본, 보이스 3개, TTS 미리듣기 | 동등 | 음성 복제는 공급자 계약 필요 |
| 다중 캐릭터 | 2인 전용 | 1인·2인 전용 + 일반 장면 최대 4명 | 우위 | 유지 |
| 제스처 | 앵글·참조·히스토리 | 동일 입력 + 영속 작업·아카이브 | 동등 | 깊은 검색은 통합 보관함 사용 |
| 배경 | 사진·스타일 변환·히스토리 | 빠른 생성 + 3단계 + 저밀도 + 히스토리 재사용 | 우위 | 완료 |
| 작업 복구 | 폴링·재접속 | Cloud Tasks·DB 상태·재시도·알림·환불 | 우위 | 외부 알림만 별도 |
| 내 생성물 | 타입 필터·24개 페이지 | 타입 필터·페이지·썸네일·용량·삭제 | 동등 | 태그/즐겨찾기는 메인 갤러리와 통합 개선 여지 |
| 프로젝트·컷 | CRUD·표지·Markdown | CRUD·표지·자산·자동 저장·AI 기획 | 동등 | 완료 |
| 문서 가져오기 | 고급 계정 PDF/DOCX/ZIP/TXT/이미지/블로그 | PDF/DOCX/ZIP/MD/TXT/CSV/HTML + 이미지 OCR + 공개 URL | 우위 | DNS 고정·사설망 차단·리다이렉트 재검사 적용 |
| 캔버스 기본 | 객체 편집·다중 컷·PNG/ZIP | 다중 컷·자동 저장·PNG/ZIP·자산 드래그 | 동등 | 완료 |
| 캔버스 고급 | Fabric 정밀 조작·AI 감지·교체 | 그룹·가이드·정렬·도형·그리기·OCR·영역 재생성 | 부분 | 캐릭터 교체, AI 자동 영역, 버전 비교가 남음 |
| 영상 | 30컷·대사·보이스·MP4 | 30컷·대사·보이스·MP4 + Veo | 우위 | 대형 프로젝트 서버 렌더링은 향후 확장 |
| 완료 외부 알림 | 카카오 알림톡 | 인앱 알림 | 부분 | 비즈채널·템플릿 심사 필요 |
| 툰 광장 | 닉네임·최신/인기·댓글·신고 | 동일 + 글/댓글 좋아요·관리자 고정 | 우위 | 완료 |
| 사용자 AI 키 | BYOK | 플랫폼 Vertex, 키 입력 불필요 | 방향 다름 | 확정된 제품 방향 |
| 크레딧 | 구독+BYOK+영상 크레딧 | 서버 요율·원장·실패 환불·충전 상품 | 방향 다름 | 결제 비활성은 현재 정상 |
| 카카오페이 | 해당 없음/다른 결제 | ready/approve 기반 코드, 운영 비활성 | 외부 결정 | 가맹점 심사와 정책 확정 뒤 활성화 |
| SNS 배포 | 다채널 흔적 | Instagram API 코드, 메뉴 비활성 | 부분 | Meta 앱 검수·토큰 운영 필요 |
| 관리자 | Plurank 대형 운영군 | 개인 창작 서비스 사용자·크레딧·지식·신고 | 방향 다름 | 전체 CRM/CX는 범위 결정 필요 |
| 캐릭터 디렉터 | 확인되지 않음 | 구조화 페르소나 설계 챗봇 | 우위 | 완료 |
| 보안 저장 | localStorage JWT | HttpOnly 쿠키, 서버 소유권, 비공개 GCS | 우위 | 유지 |

## 5. 이번 TO-BE 즉시 반영

1. 전용 제작 홈을 추가해 자산 수, 크레딧, 온보딩, 최근 작업·프로젝트를 한 화면에서 복구한다.
2. PDF/DOCX/ZIP/Markdown/TXT/CSV/HTML, 이미지 OCR, 공개 URL 자료를 크기 제한과 SSRF 방어 뒤 AI 기획 입력으로 연결한다.
3. 카카오 이메일 권한이 없는 사용자가 만든 내부 계정을 기존 이메일 계정에 명시적으로 연결한다. 콘텐츠·결제 데이터가 있는 계정은 자동 병합하지 않는다.
4. DB 지식이 비어도 챗봇이 현재 기능, 크레딧, 결제 상태를 정확히 답하도록 내장 서비스 지식을 제공한다.
5. 배경 히스토리 결과를 스타일 변환·앵글 생성의 입력으로 즉시 재사용한다.
6. 게시판 좋아요 실패 시 낙관적 UI를 되돌려 DB와 화면 수치가 달라지지 않게 한다.
7. Google Storage 전이 의존성의 보안 경고를 수정된 `uuid`로 고정하고 전체 호환성을 검증한다.

## 6. 남은 결정과 구현 경계

코드만으로 임의 확정하면 안 되는 항목:

1. **법적 문서**: 상호, 대표자, 사업자번호, 주소, 고객센터, 개인정보 담당자, 환불 기준과 법률 검토.
2. **휴대폰 본인확인**: PASS/카카오/통신사 공급자, 가입 차단 수준, 비용과 개인정보 보관 정책.
3. **다국어**: 우선 언어, 번역 책임자, 생성 프롬프트까지 번역할지 여부.
4. **카카오 알림톡**: 비즈채널, 발신 프로필, 수신 전화번호 동의, 템플릿 심사와 건당 비용.
5. **결제**: 현재 미작동이 정상. 카카오페이 가맹점 심사, 운영 CID, 약관·환불·세금·정산 정책 확정 후 활성화.
6. **SNS 게시**: Meta 앱 검수, 장기 토큰, 예약 게시 채널과 운영 책임.
7. **음성 복제**: 공급자, 화자 동의, 상업 이용·초상/성문권 계약.
8. **Plurank 확장군**: CRM·CX·마케팅·광고·업무 모듈을 개인 창작 제품에 포함할지 별도 결정.

코드상 다음 개선 후보:

- 캔버스 캐릭터 자동 교체, AI 영역 감지, 전후 비교·버전 복원
- 매우 큰 숏폼 프로젝트를 위한 서버 렌더 워커
- 보관함과 태그·즐겨찾기·프로젝트 자산의 단일 검색 인덱스

## 7. 검증 기준

- `npm test`
- `npm run lint`
- `npx tsc --noEmit`
- `npm run build`
- `npx prisma validate`
- Cloud Run 로그인·대시보드·설정·게시판·스튜디오·보관함 화면 확인
- Vertex 텍스트 1건과 이미지 1건을 실제 실행해 Cloud Tasks -> Vertex -> GCS -> DB -> 알림 흐름 확인
- Cloud Run 최근 오류 로그와 Cloud Tasks 큐 상태 확인

최신 배포·커밋·운영 상태와 후속 작업은 [project-handoff.md](./project-handoff.md)를 기준으로 한다.
